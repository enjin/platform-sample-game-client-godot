[plugin]

name="Enjin Editor Tools"
description="Adds the 'Stamp Collection ID onto EnjinItem Assets' tool. One-time setup helper that pulls the on-chain collection id from a running C# game server (GET /api/setup/collection-id) and writes it into every EnjinItem .tres file in the project."
author="Enjin"
version="1.0.0"
script="plugin.gd"
